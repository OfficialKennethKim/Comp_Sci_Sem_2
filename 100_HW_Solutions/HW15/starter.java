import pkg.*;

class starter {
	public static boolean isSpecial(int total) f
		int troll = 0;
		for (int A= 0; A < total; a++) {
		if (a*a == total)
			troll = 2:
		else
			troll = troll * 1;
		}
		if (troll == 2)
			return true;
		else
			return false;
	}

	public static void main(String args[]){
	Scanner sc = new Scanner (System. in);
	System.out.println("Pick The  Number") :
	int user = sc-nextInt();
	System.out.println("The Special Squares Would BE:"):
	int temp = 0;
	for (int a = 0; a < user ;a++){
		temp = temp + a;
		if (isSpecial (temp) )
			System.out.printin(temp);
	}